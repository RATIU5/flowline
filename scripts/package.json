{
  "name": "@flowline/scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "bun run src/check-versions.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "dependencies": {
    "@effect/platform-bun": "catalog:effect",
    "effect": "catalog:effect"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "catalog:typescript"
  }
}
