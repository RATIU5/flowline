{
  "name": "@flowline/examples-forms",
  "private": true,
  "version": "0.0.1",
  "description": "Example forms for Flowline",
  "main": "dist/app.js",
  "scripts": {
    "exe": "bun src/server.ts",
    "build": "buildkit build src/app.ts --bundle --platform=browser",
    "typecheck": "tsgo --noEmit --project tsconfig.json"
  },
  "dependencies": {
    "effect": "3.17.9",
    "@flowline/forms": "workspace:*",
    "@flowline/forms-vanilla": "workspace:*",
    "typescript": "5.9.2"
  },
  "devDependencies": {
    "@flowline/buildkit": "workspace:*"
  }
}
